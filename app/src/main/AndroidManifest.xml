<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.barbie.simanartest">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TextViewButtonActivity" />
        <activity android:name=".EditTextActivity" />
        <activity android:name=".SpinnerActivity" />
        <activity android:name=".AlertDialogActivity" />
        <activity android:name=".ToastActivity" />
        <activity android:name=".DateTimeActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".customlistview.CustomListViewActivity" />
        <activity android:name=".RecycleViewActivity" />
        <activity android:name=".sendandreceive.SendDataActivity" />
        <activity android:name=".ImageActivity" />
        <activity android:name=".database.MySQLiteActivity" />
        <activity android:name=".server.ConnectServerActivity" />
        <activity android:name=".MemberActivity" />
        <activity android:name=".sendandreceive.FormActivity" />
        <activity android:name=".database.FormDatabaseActivity" />
        <activity android:name=".CheckBoxActivity"></activity>
    </application>

</manifest>